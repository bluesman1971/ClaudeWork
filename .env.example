# ── Trip Master — Environment Variables ─────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it is listed in .gitignore.

# ── API Keys ─────────────────────────────────────────────────────────────────
GOOGLE_PLACES_API_KEY=your-google-places-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ── Flask ─────────────────────────────────────────────────────────────────────
FLASK_ENV=production          # Use 'development' only for local dev
FLASK_DEBUG=False
PORT=5001
HOST=127.0.0.1

# ── Auth & persistence ────────────────────────────────────────────────────────
# Generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-to-a-long-random-string

# Database:
#   - SQLite (default, local dev only — not suitable for multi-worker production):
DATABASE_URL=sqlite:///trip_master.db
#   - PostgreSQL (recommended for production — uncomment and set your URI):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Scout model ───────────────────────────────────────────────────────────────
# Override to switch all three scouts to a new model without code changes.
# Defaults to claude-haiku-4-5-20251001 if unset.
# SCOUT_MODEL=claude-haiku-4-5-20251001
# SCOUT_MODEL_LABEL=Claude Haiku 4.5

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Defaults to localhost for dev. Set this for production, e.g.:
# CORS_ORIGINS=https://tripmaster.example.com,https://www.tripmaster.example.com
# CORS_ORIGINS=
